{
  "name": "Imba-WebExt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun scripts/build.js --watch --mode=development",
    "dev:chrome": "BROWSER=chrome bun scripts/build.js --watch --mode=development",
    "dev:firefox": "BROWSER=firefox bun scripts/build.js --watch --mode=development",
    "build": "bun scripts/build.js --mode=development",
    "build:chrome": "BROWSER=chrome bun scripts/build.js --mode=development",
    "build:firefox": "BROWSER=firefox bun scripts/build.js --mode=development",
    "prod": "bun run zip:chrome && bun run zip:firefox",
    "zip:chrome": "BROWSER=chrome bun scripts/build.js --mode=production && bun scripts/zip.js --browser=chrome",
    "zip:firefox": "BROWSER=firefox bun scripts/build.js --mode=production && bun scripts/zip.js --browser=firefox",
    "test": "bun run scripts/build-tests.js && bun test test.local",
    "test:watch": "bun run scripts/watch-tests.js && bun test test.local"
  },
  "dependencies": {
    "turndown": "^7.1.2",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "imba": "^2.0.0-alpha.247",
    "vite": "^7.2.0",
    "vite-plugin-imba": "^0.10.2"
  }
}