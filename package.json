{
  "name": "Imba-WebExt",
  "scripts": {
    "dev:imba": "imba build --esm --force -w index.html -d -o dist -M --base .",
    "build:imba": "imba build --esm index.html -o dist/ -M --base ."
  },
  "devDependencies": {
    "imba": "^2.0.0-alpha.247"
  }
}
